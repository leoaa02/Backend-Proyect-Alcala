<h1>Lista de Productos</h1>

    <ul>
    {{#each products}}
        <li>
        <strong>{{title}}</strong> - ${{price}} <br />
        {{description}} (Stock: {{stock}})
    </li>
    {{/each}}
</ul>
